export const ACCOUNT_REDIRECT = {
  redirect: {
    destination: "/account",
    permanent: false,
  },
};

export const LOGIN_REDIRECT = {
  redirect: {
    destination: "/auth/login",
    permanent: false,
  },
};

export const ADD_HOST_REDIRECT = {
  redirect: {
    destination: "/account/addHost",
    permanent: false,
  },
};
